$('.flash-notice').replaceWith("<%= j render partial: 'partials/flash' %>");
retriggerFlash();

<% if @success %>
	$('#purchase-order-items-list').html("<%= j render partial: 'purchase_order_item', supplier: @supplier, purchase_order: @purchase_order %>");
<% end %>