$('.flash-notice').replaceWith("<%= j render partial: 'partials/flash' %>");
retriggerFlash();

$('.modal').modal('hide');
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();
$('#purchases').html("<%= react_component 'Purchases', { branchUser: branch_admin?, clientOrBrandAdmin: client_admin? || brand_admin? } %>");
