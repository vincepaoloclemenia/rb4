<!DOCTYPE html>
<html>
    <head>
        <title>Purchase Order</title>
            <%= wicked_pdf_stylesheet_link_tag 'style' %>
            <%= wicked_pdf_stylesheet_link_tag 'pdf' %>
        <meta charset='utf-8'>
    </head>
    <body id='pdf-body'>
        <div class='pdf-container'>
            <h4><%= @purchase_order.brand.name %></h4>
            <br>
            <div class='row mb10'>
                <div class='col-xs-5'>
                    <p class='header'>Supplier:</p>                    
                    <p class='header'>Purchase Order Number:</p>
                    <p class='header'>Date:</p>
                </div>
                <div class='col-xs-6'>
                    <p><%= @purchase_order.supplier.name %></p>
                    <p><%= @purchase_order.po_number %></p>
                    <p><%= @purchase_order.po_date.strftime('%B %d, %Y') %></p>
                </div>              
            </div>
            <br>
            <h5>Items Purchased</h5>
            <table class='table table-bordered table-striped mb0'>
                <thead>
                    <tr class='bg-thead'>
                        <th>Item Name</th>
                        <th>Remarks</th>
                        <th>Quantity</th>
                        <th>Unit</th>
                        <th>Unit Price</th>
                        <th>Total Amount</th>
                    </tr>
                </thead>
                <tbody>
                    <% @po_items.map do |poi| %>
                        <tr>
                            <td><%= poi.item.name %></td>
                            <td><%= poi.remarks == '' ? '---' : poi.remarks %></td>
                            <td><%= poi.quantity.to_i.to_s %></td>
                            <td><%= poi.unit.name %></td>
                            <td><%= to_peso(poi.price_selected) %></td>
                            <td><%= to_peso(poi.total_amount) %></td>
                        </tr>
                    <% end %>
                    <tr>
                        <td colspan='4'>Total</td>
                        <td colspan='2'><%= to_peso(@po_items.pluck(:total_amount).sum) %></td>
                    </td>
                </tbody>
            </table>
        </div>
    </body>
</html>